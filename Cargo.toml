[package]
name = "bevy-jam-4"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = {version = "0.12.0", features = ["serialize", "file_watcher"]}
serde = { version = "*" }
serde_json = { version = "*" }
